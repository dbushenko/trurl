database {
  name = "{{ProjectName}}"
  user = "{{ProjectName}}"
  password = "{{ProjectName}}"
  host = "localhost"
  port = 3306
  salt = "my-salt-123"
}

application {
  port = 3000
  poolSize = 10
}